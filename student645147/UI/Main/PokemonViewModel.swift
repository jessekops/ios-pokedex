////  PokemonViewModel.swift//  student645147////  Created by user271057 on 11/18/24.//import Foundationclass PokemonViewModel: ObservableObject {    @Published var pokemons: [Pokemon] = []        func fetchPokemons() {        guard let url = URL(string: "https://pokeapi.co/api/v2/pokemon/?limit=20") else { return }                URLSession.shared.dataTask(with: url) { data, response, error in            guard let data = data, error == nil else {                print("Error fetching Pokémon: \(error?.localizedDescription ?? "Unknown error")")                return            }                        do {                let response = try JSONDecoder().decode(PokemonResponse.self, from: data)                DispatchQueue.main.async {                    self.pokemons = ModelMapper.mapResponseToPokemon(response: response)                }            } catch {                print("Error decoding Pokémon response: \(error)")            }        }.resume()    }}